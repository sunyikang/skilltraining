// Reference to https://github.com/vadimcn/vscode-lldb
{
    "version": "0.2.0",
    "configurations": [
      {
        "name": "Launch",
        "type": "lldb",
        "request": "launch",
        "program": "${workspaceFolder}/sandbox/hello.out",
        "args": [],
        "cwd": "${workspaceFolder}",
      }
    ]
}
