// For debugging (below settings), details reference to https://github.com/vadimcn/vscode-lldb
// For running, run the executive file directly. e.g. leetcode/70_climbingstairs.out 
{
    "version": "0.2.0",
    "configurations": [
      {
        "name": "Launch",
        "type": "lldb",
        "request": "launch",
        "program": "${workspaceFolder}/exam_virtuos/15.out",
        "args": [],
        "cwd": "${workspaceFolder}"
      }
    ]
}
