// Reference to https://github.com/vadimcn/vscode-lldb
{
    "version": "0.2.0",
    "configurations": [
      {
        "name": "Launch",
        "type": "lldb",
        "request": "launch",
        "program": "${workspaceFolder}/algorithm/arrayaddition.out",
        "args": [],
        "cwd": "${workspaceFolder}"
      }
    ]
}
